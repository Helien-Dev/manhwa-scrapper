[project]
name = "manhwa-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alvaro", email = "alvaroaviladev@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "patchright>=1.61.2",
    "playwright>=1.62.0",
    "pydantic-settings>=2.15.0",
    "requests>=2.34.2",
]

[project.scripts]
manhwa-scraper = "manhwa_scraper:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
